.carrousel {
  display: flex;
  overflow: auto;
  -webkit-overflow-scrolling: touch;

  &,
  li {
    padding: 0;
    list-style: none;
  }

  li {
    flex: 0 0 90%;
  }

  * {
    height: 100%;
  }
  img {
    width: 100%;
    height: auto;
  }
}
