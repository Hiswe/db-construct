.page-faq {
  h1 {
    background: url('/image/1400x200');
    background-size: cover;
    font-text();
    font-size: 20px;
    padding: $half-gutter;
  }
  dl {
    // max-width: col(2);
    padding: $half-gutter;

    +mq('>=M') {
      margin: 0 20%;
    }
  }
  dt {
    font-weight: bold;
    margin-bottom: 1em;
    font-title();
  }

  dd + dt {
    margin-top: 3em;
  }
}
