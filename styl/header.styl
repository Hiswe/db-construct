.main-header {
  text-align: left;
  max-width: $large-range;
  width: 100%;
  margin: 0 auto;
  padding: 0 $half-gutter;
  container();

  +mq('>=M') {
    display: flex;
    padding: 0;
    height: 80px;
  }

  +mq('>=L') {
    margin-top: $gutter;
    position: relative;
    z-index: $zindex--header;
  }

  input {
    display: none;
  }
  .toggle-menu {
    position: fixed;
    top: $half-gutter;
    right: $gutter;
    background: $accent;
    flex-center-align();
    width: 40px;
    height: @width;
    cursor: pointer;
    z-index: $zindex--hamburger;

    +mq('>=M') {
      display: none;
    }
  }
  input:checked ~ label {
    background: $bg;
  }
  input:not(:checked) ~ label .icon-close,
  input:checked ~ label .icon-menu {
    display: none;
  }
}

.logo {
  display: inline-block;
  padding: $half-gutter 0;
  align-self: flex-end;


  +mq('>=M') {
    padding: 0;
    margin-right: ($gutter * 2);
  }

  svg {
    +mq('==XS') {
      display: inline;
      width: 50%;
      height: 40px;
    }
    +mq('==S') {
      display: inline;
      width: 50%;
      height: 40px;
    }
    +mq('==M') {
      height: 60px;
      width: 230px * 0.75;
    }
  }
}


nav {
  display: none;
  background: $primary;
  flex: 1 0 auto;

  input:checked ~ & {
    display: flex;
  }

  +mq('<M') {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: $zindex--menu-mobile;
    flex-wrap: wrap;
  }

  +mq('==M') {
    padding-right: $gutter;
    margin-right: $gutter * -1;
  }

  +mq('>=M') {
    display: flex;
    justify-content: flex-end;
  }

  a {
    padding: 2.75em 1em 0;
    color: white;
    text-decoration: none;

    +mq('<M') {
      flex: 0 0 50%;
      flex-center-align();
      text-transform: uppercase;
      padding: 0;

      &.is-selected {
        background: rgba(white, .2);
        pointer-events: none;
        cursor: normal;
      }

      &:nth-child(even) {
        border-left: 1px solid rgba(white, .2);
      }
      &:nth-child(2) ~ a {
        border-top: 1px solid rgba(white, .2);
      }
    }

    +mq('>=M') {
      border-bottom: 4px solid transparent;

      &.is-selected {
        border-bottom-color: $accent;
      }

      &:hover {
        background: rgba(white, .2);
      }
    }
  }
}
