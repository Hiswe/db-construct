*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  // &[lang=th] {
  //   font-size: 20px;
  // }
  background: #302D2C;
  color: white;
  font-size: $fs;
  font-text();
}

body {
  container();
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

main {
  display: block;
  flex: 1 0 auto;
}

img {
  max-width: 100%;
  display: block;
}


h1,
h2 {
  font-title();
}
